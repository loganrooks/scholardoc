---
phase: 00-clean-up-workspace-and-organize-git-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - spikes/sample_pdfs/MANIFEST.md
  - ground_truth/DIAGNOSTIC_PLAN.md
  - ground_truth/IMPLEMENTATION_PLAN.md
  - ground_truth/README.md
autonomous: true
requirements: []  # Phase 0 is housekeeping — no requirement IDs in ROADMAP

must_haves:
  truths:
    - "spikes/sample_pdfs/*.pdf files are gitignored and untracked"
    - "spikes/output/ is gitignored"
    - "A MANIFEST.md in spikes/sample_pdfs/ documents all 20 expected PDFs with filenames, subjects, and sources"
    - "ground_truth superseded planning docs (DIAGNOSTIC_PLAN.md, IMPLEMENTATION_PLAN.md, README.md) are committed"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules for PDFs and generated output"
      contains: "spikes/output/"
    - path: "spikes/sample_pdfs/MANIFEST.md"
      provides: "Documentation of expected sample PDF corpus"
      min_lines: 25
  key_links:
    - from: ".gitignore"
      to: "spikes/sample_pdfs/"
      via: "*.pdf global rule (no exception for sample_pdfs)"
      pattern: "^\\*\\.pdf$"
---

<objective>
Update .gitignore to exclude sample PDFs (131MB) and generated spike output from version control, create a manifest documenting the expected PDF corpus, and commit the three untracked ground_truth planning documents that are superseded by the roadmap but valuable as prior-art reference.

Purpose: Remove large binaries from git tracking and ensure reproducibility by documenting the local PDF corpus. Commit ground_truth planning docs so they are preserved as historical reference for later phases.
Output: Clean .gitignore, PDF manifest, committed ground_truth docs
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and untrack PDFs</name>
  <files>.gitignore</files>
  <action>
Edit .gitignore to:
1. Remove the line `!spikes/sample_pdfs/*.pdf` (this exception currently allows PDFs in sample_pdfs to be tracked — we want them ignored)
2. Add `spikes/output/` to the "Project specific" section (generated spike results, not version-controlled)
3. Keep the existing `*.pdf` global rule (it already ignores PDFs project-wide)

After editing .gitignore, untrack the 18 currently tracked PDFs:
```bash
git rm --cached 'spikes/sample_pdfs/*.pdf'
```
This removes them from git index without deleting the local files.

Then create `spikes/sample_pdfs/MANIFEST.md` documenting all 20 PDFs in the corpus. Include:
- A header explaining these are local-only test corpus files (not version-controlled)
- A table with columns: Filename | Subject/Author | Notes
- The 20 files are (list each by exact filename from the directory listing):
  - Acts of Religion (Jacques Derrida) (2016) (Z-Library).pdf
  - Aristotle- Nicomachean Ethics (Aristotle) (—) (Z-Library).pdf
  - ComayRebecca_MourningSickness_HegelAndTheFrenchRevolution.pdf
  - derrida_footnote_pages_120_125.pdf
  - Derrida_MarginsOfPhilosophy.pdf
  - Derrida_TheBeastAndTheSovereignVol1.pdf
  - Derrida_TheTruthInPainting.pdf
  - Derrida_WritingAndDifference.pdf
  - Dissemination (Jacques Derrida) (Z-Library).pdf
  - Heidegger_BeingAndTime.pdf
  - Heidegger_DiscourseOnThinking.pdf
  - heidegger_pages_17-24_full_translator_preface.pdf
  - heidegger_pages_22-23_primary_footnote_test.pdf
  - Heidegger_Pathmarks.pdf
  - Kant_CritiqueOfJudgement.pdf
  - kant_critique_pages_64_65.pdf
  - Lenin_StateAndRevolution.pdf
  - Monolingualism of the Other or, The Prosthesis of Origin (Cultural Memory in the Present) (Jacques Derrida) (Z-Library).pdf
  - Plato Complete Works (Plato, John M. Cooper, D. S. Hutchinson) (1997) (Z-Library).pdf
  - Rogues Two Essays on Reason (Meridian Crossing Aesthetics) (Jacques Derrida) (Z-Library).pdf

Note in the manifest: 3 files are page excerpts (derrida_footnote_pages, heidegger_pages_17-24, heidegger_pages_22-23, kant_critique_pages) used for targeted spike testing. The rest are full scholarly texts — primarily philosophy (Derrida, Heidegger, Kant, Plato, Aristotle, Lenin, Comay).
  </action>
  <verify>
Run `git status` — the 18 previously tracked PDFs should appear as deleted (from index), the 2 previously untracked PDFs should no longer appear as untracked, and spikes/output/ should not appear. MANIFEST.md should show as a new untracked file (will be committed in Task 2).
  </verify>
  <done>.gitignore updated, 18 PDFs untracked from git, MANIFEST.md created with all 20 PDFs documented</done>
</task>

<task type="auto">
  <name>Task 2: Commit gitignore changes, manifest, and ground_truth docs</name>
  <files>.gitignore, spikes/sample_pdfs/MANIFEST.md, ground_truth/DIAGNOSTIC_PLAN.md, ground_truth/IMPLEMENTATION_PLAN.md, ground_truth/README.md</files>
  <action>
Create two logical commits:

**Commit 1: Gitignore and PDF manifest**
```bash
git add .gitignore spikes/sample_pdfs/MANIFEST.md
# The `git rm --cached` from Task 1 already staged the PDF removals
git commit -m "chore: gitignore sample PDFs and spike output, add PDF manifest

- Remove !spikes/sample_pdfs/*.pdf exception from .gitignore
- Untrack 18 previously committed PDFs (131MB removed from git)
- Add spikes/output/ to .gitignore (generated spike results)
- Create spikes/sample_pdfs/MANIFEST.md documenting 20 test corpus PDFs"
```

**Commit 2: Ground truth superseded planning docs**
```bash
git add ground_truth/DIAGNOSTIC_PLAN.md ground_truth/IMPLEMENTATION_PLAN.md ground_truth/README.md
git commit -m "docs(ground-truth): commit superseded planning docs for reference

These planning documents are superseded by .planning/ROADMAP.md but
contain prior-art context valuable for Phase 1 (schema), Phase 3
(testing methodology), and Phase 4 (annotation tool) design work.

- DIAGNOSTIC_PLAN.md: Early ground truth diagnostic analysis
- IMPLEMENTATION_PLAN.md: Original implementation strategy
- README.md: Ground truth directory overview"
```
  </action>
  <verify>
Run `git log --oneline -2` to confirm both commits exist with correct messages. Run `git status` to confirm no remaining changes related to .gitignore, spikes/sample_pdfs/, or ground_truth/.
  </verify>
  <done>Two clean commits: one for gitignore/manifest, one for ground_truth docs. No untracked PDFs or output in status.</done>
</task>

</tasks>

<verification>
- `git diff --cached` shows nothing (no staged changes left)
- `git status` shows spikes/sample_pdfs/*.pdf files are NOT listed as untracked
- `git status` shows spikes/output/ is NOT listed as untracked
- `git log --oneline -2` shows the two commits
- `cat spikes/sample_pdfs/MANIFEST.md` lists all 20 PDF files
- `git show --stat HEAD~1` shows .gitignore, MANIFEST.md, and 18 PDF deletions
- `git show --stat HEAD` shows the 3 ground_truth files
</verification>

<success_criteria>
- PDFs no longer tracked by git (verified via `git ls-files spikes/sample_pdfs/` returns only MANIFEST.md)
- spikes/output/ does not appear in `git status`
- MANIFEST.md exists and documents all 20 PDFs
- ground_truth superseded docs are committed
</success_criteria>

<output>
After completion, create `.planning/phases/00-clean-up-workspace-and-organize-git-backlog/00-01-SUMMARY.md`
</output>
