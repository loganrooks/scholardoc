---
phase: 01.1-schema-taxonomy-review-revision
task: 0
total_tasks: 5
status: pre-execution (replanning needed)
last_updated: 2026-02-19T16:21:16.206Z
---

<current_state>
Phase 1.1 has 5 plans written but NONE executed (0/5). Before executing, we ran a gap
analysis (Quick Task 1) comparing the planned schema against a newly inventoried corpus
of ~5,272 pages of structurally difficult texts (85 phenomena, 9 categories). The gap
analysis was too conservative — it rated everything as "degraded" or "nice-to-have" and
relied on `extra="allow"` as a crutch. User wants a SECOND, non-conservative gap analysis
that considers deeper structural changes to the schema, not just additive field patches.

The first gap analysis is preserved at:
`.planning/phases/01.1-schema-taxonomy-review-revision/01.1-GAP-ANALYSIS.md`

The user explicitly wants both analyses side-by-side for comparison.
</current_state>

<completed_work>

- Quick Task 1: Conservative gap analysis (01.1-GAP-ANALYSIS.md) — DONE but insufficient
  - Mapped all 85 phenomena across 9 categories to Phase 1.1 schema
  - Found 0 blocking, 5 degraded, 6 nice-to-have gaps
  - Recommendation was too conservative ("proceed as planned, use extra fields")
  - Commits: d52e0ef, 358691d
- Corpus inventory and structural phenomena catalogue created by user:
  - `spikes/sample_pdfs/DifficultTexts/samples/structural_phenomena.md` (~85 phenomena)
  - `spikes/sample_pdfs/DifficultTexts/samples/corpus_inventory.md` (15 texts, ~5,272 pages)
  - Sample PDFs are in `samples/test_files` within DifficultTexts (user clarification)
</completed_work>

<remaining_work>

1. **Rerun gap analysis (non-conservative)** — NEW quick task needed
   - Must consider structural changes: new models, model redesign, not just field additions
   - Every "Partial" coverage should aim for "Full" — don't settle for workarounds
   - Produce as `01.1-GAP-ANALYSIS-v2.md` (keep v1 for comparison)
   - Key areas needing deeper structural thought:
     a. Region model needs column/register/reading-stream concept (not just children)
     b. Text direction (RTL/bidi) should be a core property, not metadata
     c. FormattingAnnotation is span-level but some properties are region-level
     d. Talmudic page layout is a first-class paradigm (~30% of corpus)
     e. WHERE on page = semantic information (Rashi=inner margin, Tosafot=outer)
     f. Script variant (Rashi vs square Hebrew) needs explicit modeling
     g. Color as semantic signal needs first-class support

2. **Decision: replan Phase 1.1 or add Phase 1.2**
   - Based on v2 gap analysis results
   - Phase 1.1 plans can be rewritten since nothing is executed yet
   - Or Phase 1.2 can be added for structural changes that don't fit in 1.1

3. **Execute the revised plans**
   - After replanning, execute Phase 1.1 (and 1.2 if created)
</remaining_work>

<decisions_made>

- [User]: Gap analysis v1 is too conservative — "nice-to-have" beside "Partial" is not acceptable, we want Full coverage where possible
- [User]: Don't erase the first gap analysis — keep both side by side
- [User]: Consider wider structural changes, not just additive recommendations
- [User]: Phase 1.1 is not executed yet, so plans can be revised or rewritten
- [User]: Need to clear context before rerunning the gap analysis
</decisions_made>

<blockers>
None — this is a strategic decision point, not a technical blocker.
</blockers>

<context>
The user has assembled a remarkable corpus of structurally difficult texts spanning
Derrida (dual-column, multi-register), Talmudic texts (commentary-around-central-text,
Rashi script, RTL), classical texts (Greek critical apparatus, OCR-only scans), and
philosophy monographs (dual pagination, multi-language). The corpus demands that our
GT schema handle these phenomena as FIRST-CLASS structural concepts, not as afterthoughts
patched with extra fields.

The key insight the user is driving at: the schema should be DESIGNED for this corpus,
not merely tolerant of it. The "extra fields cover everything" argument misses the point —
if 30% of your pages are RTL with multi-layer commentary in Rashi script, that's not an
edge case, it's a core use case that deserves dedicated modeling.

Areas where the current Phase 1.1 plans may need structural rethinking:
1. Region model — needs reading stream / column identity concept
2. Text direction — should be on Region or Page, not just derivable from language tags
3. Formatting model — some properties (typeface, script, direction) are region-level, not span-level
4. Commentary apparatus — the Talmudic page layout paradigm needs dedicated support
5. Color semantics — needs first-class FormattingType
6. Spatial-semantic coupling — position on page carries semantic meaning in these texts
</context>

<next_action>
1. /clear — fresh context window
2. /gsd:quick — rerun gap analysis as Quick Task 2 with non-conservative mandate:
   - Produce `01.1-GAP-ANALYSIS-v2.md` alongside existing v1
   - For every "Partial" or "degraded" gap, propose a STRUCTURAL fix (new model, model redesign)
   - Don't settle for "extra fields cover it"
   - Consider: What would the schema look like if we designed it FOR this corpus?
3. Based on v2 results, decide: replan Phase 1.1 or add Phase 1.2
</next_action>
