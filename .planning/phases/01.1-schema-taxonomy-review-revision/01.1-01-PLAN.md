---
phase: 01.1-schema-taxonomy-review-revision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scholargt/schema/base.py
  - scholargt/schema/labels.py
  - scholargt/schema/version.py
autonomous: true
requirements:
  - SCH-01

must_haves:
  truths:
    - "LocationRef model exists for consistent cross-element referencing with page, region_id, char_offset, char_length"
    - "SpatialLabel has exactly 21 values organized for visual layout detection, including NOTE_AREA, NOTE_CONTINUATION, TOC_AREA, ABSTRACT, CODE_BLOCK, INDEX_AREA (SFP-5), and UNKNOWN"
    - "SemanticType has exactly 9 values with NOTE replacing FOOTNOTE+ENDNOTE and COMMENTARY added"
    - "FormattingType has 9 values including SUBSCRIPT, MONOSPACE, and COLOR (SFP-4)"
    - "ReferenceSystem enum has 13 values including CATCHWORD (SFP-6)"
    - "CitationFormat enum has 5 values decomposed from old CitationType"
    - "CitationStyle enum exists at document level"
    - "ScriptVariant enum has 6 values including SQUARE_HEBREW, RASHI_SCRIPT, FRAKTUR (SFP-3)"
    - "Old enums removed: ScanQuality, Difficulty, CitationType, MarginalRefType"
    - "DocumentType renamed to DocumentSectionType"
    - "Schema version bumped to 2.0.0"
  artifacts:
    - path: "scholargt/schema/base.py"
      provides: "LocationRef model + existing BBox, GTElement, VerificationRecord"
      contains: "class LocationRef"
    - path: "scholargt/schema/labels.py"
      provides: "All label enums for the universal annotation taxonomy"
      contains: "class ScriptVariant"
    - path: "scholargt/schema/version.py"
      provides: "Schema version 2.0.0"
      contains: "SCHEMA_VERSION = \"2.0.0\""
  key_links:
    - from: "scholargt/schema/labels.py"
      to: "scholargt/schema/base.py"
      via: "no dependency (labels imports nothing from base)"
      pattern: "from enum import Enum"
    - from: "scholargt/schema/base.py"
      to: "pydantic"
      via: "LocationRef inherits BaseModel"
      pattern: "class LocationRef\\(BaseModel\\)"
---

<objective>
Foundation: Add LocationRef to base.py, overhaul all label enums in labels.py (incorporating SFP-3, SFP-4, SFP-5, SFP-6), and bump schema version to 2.0.0.

Purpose: Every downstream model in Plans 02-05 depends on these enums and base types. LocationRef standardizes the scattered page/region_id/char_offset/char_length pattern used by Note, Citation, Commentary, SousRature, CrossReference, MarginalReference, and FormattingAnnotation. The enum overhaul restructures the taxonomy by downstream evaluation task and incorporates the six adopted Structural Fix Proposals.

Output: Three modified files providing the type foundation for the entire v2.0.0 schema.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-CONTEXT.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-RESEARCH.md
@scholargt/schema/base.py
@scholargt/schema/labels.py
@scholargt/schema/version.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LocationRef model to base.py</name>
  <files>scholargt/schema/base.py</files>
  <action>
Add a `LocationRef` class to `scholargt/schema/base.py` AFTER the `BBox` class and BEFORE `VerificationRecord`.

```python
class LocationRef(BaseModel):
    """Reference to a specific location in the GT corpus.

    Standardizes the scattered page/region_id/char_offset/char_length pattern
    used across semantic elements. Use for body_marker, content_marker,
    target_location, and any element that references a position in a page region.
    """
    page: int = Field(description="0-based page index")
    region_id: str = Field(description="Region ID on the page")
    char_offset: int | None = Field(default=None, description="Character offset within region text")
    char_length: int | None = Field(default=None, description="Length of referenced text span")
```

Do NOT modify BBox, VerificationRecord, or GTElement. They remain unchanged.
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "from scholargt.schema.base import LocationRef; lr = LocationRef(page=0, region_id='r1'); print(lr.model_json_schema())"`
Should print JSON schema with page (required int), region_id (required str), char_offset (optional int), char_length (optional int).
  </verify>
  <done>LocationRef importable from scholargt.schema.base with all 4 fields, page and region_id required, char_offset and char_length optional with None defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul all label enums in labels.py</name>
  <files>scholargt/schema/labels.py</files>
  <action>
COMPLETELY REWRITE `scholargt/schema/labels.py`. The new file must contain these enums in this order:

1. **SpatialLabel** (21 values) -- WHERE on the page. Replace FOOTNOTE_AREA, ENDNOTE_AREA, FOOTNOTE_CONTINUATION with NOTE_AREA, NOTE_CONTINUATION. Add TOC_AREA, ABSTRACT, CODE_BLOCK, INDEX_AREA (SFP-5), UNKNOWN. Keep all other existing values.
   Values: TEXT_BLOCK, NOTE_AREA, NOTE_CONTINUATION, PAGE_HEADER, PAGE_FOOTER, PAGE_NUMBER, SECTION_HEADER, TITLE, BLOCK_QUOTE, LIST_ITEM, TABLE, FIGURE, CAPTION, FORMULA, MARGINAL_NOTE, BIBLIOGRAPHY_AREA, TOC_AREA, ABSTRACT, CODE_BLOCK, INDEX_AREA, UNKNOWN

2. **SemanticType** (9 values) -- WHAT it means. Replace FOOTNOTE+ENDNOTE with NOTE. Add COMMENTARY. Keep all others.
   Values: NOTE, CITATION, BIBLIOGRAPHY_ENTRY, SECTION, SOUS_RATURE, CROSS_REFERENCE, MARGINAL_REFERENCE, PAGE_NUMBER_ANNOTATION, COMMENTARY

3. **FormattingType** (9 values) -- HOW text is decorated. Add SUBSCRIPT, MONOSPACE, COLOR (SFP-4). Keep all existing.
   Values: BOLD, ITALIC, UNDERLINE, STRIKETHROUGH, SMALL_CAPS, SUPERSCRIPT, SUBSCRIPT, MONOSPACE, COLOR

4. **ScriptVariant** (6 values) -- NEW enum (SFP-3). Script variants where BCP 47 is insufficient.
   Values: SQUARE_HEBREW, RASHI_SCRIPT, NASTALIQ, NASKH, FRAKTUR, CUSTOM
   Docstring must explain: primary use is Rashi vs square Hebrew, both "he-Hebr" in BCP 47 but typographically distinct.

5. **CitationFormat** (5 values) -- NEW enum. How citation appears in text. Replaces old CitationType.
   Values: PARENTHETICAL, NUMERIC, INLINE_AUTHOR, NOTE_BASED, AUTHOR_TITLE

6. **ReferenceSystem** (13 values) -- NEW enum. What coordinates locate the passage. Shared by Citation and MarginalReference, eliminating old MarginalRefType duplication.
   Values: STANDARD, STEPHANUS, BEKKER, AKADEMIE, AB_EDITION, PARAGRAPH, SZ_PAGINATION, DIELS_KRANZ, LINE_NUMBER, CHAPTER_VERSE, LEGAL, CATCHWORD (SFP-6), CUSTOM

7. **CitationStyle** (7 values) -- NEW enum. Document-level citation convention.
   Values: APA, CHICAGO_NB, CHICAGO_AD, MLA, VANCOUVER, TURABIAN, CUSTOM

8. **DocumentSectionType** (5 values) -- RENAMED from DocumentType (collision with DocumentSource.document_type Literal). Same values.
   Values: METADATA, TOC, FRONT_MATTER, BACK_MATTER, NOTE_SCHEMA

**REMOVE entirely:** ScanQuality, Difficulty, CitationType, MarginalRefType. These are eliminated by the redesign.

All enums use `(str, Enum)` pattern for JSON string serialization. Add clear docstrings to each enum explaining its role and the downstream evaluation task it supports.

Module docstring should reflect: 8 enums organized by dimension (spatial, semantic, formatting, script, citation format, reference system, citation style, document structure).
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
from scholargt.schema.labels import (
    SpatialLabel, SemanticType, FormattingType, ScriptVariant,
    CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType
)
assert len(SpatialLabel) == 21, f'SpatialLabel has {len(SpatialLabel)}'
assert len(SemanticType) == 9, f'SemanticType has {len(SemanticType)}'
assert len(FormattingType) == 9, f'FormattingType has {len(FormattingType)}'
assert len(ScriptVariant) == 6, f'ScriptVariant has {len(ScriptVariant)}'
assert len(CitationFormat) == 5, f'CitationFormat has {len(CitationFormat)}'
assert len(ReferenceSystem) == 13, f'ReferenceSystem has {len(ReferenceSystem)}'
assert len(CitationStyle) == 7, f'CitationStyle has {len(CitationStyle)}'
assert len(DocumentSectionType) == 5, f'DocumentSectionType has {len(DocumentSectionType)}'
# SFP values present
assert SpatialLabel.INDEX_AREA.value == 'index_area'
assert FormattingType.COLOR.value == 'color'
assert ScriptVariant.RASHI_SCRIPT.value == 'rashi_script'
assert ReferenceSystem.CATCHWORD.value == 'catchword'
print('All enum checks passed')
"
`
Also verify old enums are gone: `uv run python -c "from scholargt.schema.labels import CitationType" 2>&1 | grep -q "cannot import" && echo "Old enum correctly removed"` (should print "Old enum correctly removed").
  </verify>
  <done>labels.py contains exactly 8 enums with correct value counts. Old enums (ScanQuality, Difficulty, CitationType, MarginalRefType) are removed. SFP-3 (ScriptVariant), SFP-4 (COLOR in FormattingType), SFP-5 (INDEX_AREA in SpatialLabel), SFP-6 (CATCHWORD in ReferenceSystem) all present. DocumentType renamed to DocumentSectionType.</done>
</task>

<task type="auto">
  <name>Task 3: Bump schema version to 2.0.0</name>
  <files>scholargt/schema/version.py</files>
  <action>
Update `scholargt/schema/version.py`:
- `SCHEMA_VERSION = "2.0.0"`
- `SCHEMA_VERSION_TUPLE = (2, 0, 0)`
- `MIN_SUPPORTED_VERSION = "2.0.0"` (breaking change -- old GT files need migration)

This is a major version bump signaling incompatibility: renamed enums, removed models, changed field types.
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "from scholargt.schema.version import SCHEMA_VERSION; assert SCHEMA_VERSION == '2.0.0'; print('Version bumped to 2.0.0')"`
  </verify>
  <done>SCHEMA_VERSION is "2.0.0", SCHEMA_VERSION_TUPLE is (2, 0, 0), MIN_SUPPORTED_VERSION is "2.0.0".</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `uv run python -c "from scholargt.schema.base import LocationRef, BBox, GTElement, VerificationRecord"` -- all base models importable
2. `uv run python -c "from scholargt.schema.labels import SpatialLabel, SemanticType, FormattingType, ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType"` -- all 8 enums importable
3. `uv run python -c "from scholargt.schema.version import SCHEMA_VERSION; assert SCHEMA_VERSION == '2.0.0'"` -- version correct

NOTE: Downstream modules (spatial.py, semantic.py, etc.) will have BROKEN IMPORTS after this plan because they still reference old enum names (CitationType, MarginalRefType, etc.). This is expected -- Plan 02 fixes those imports as part of the model rewrite.
</verification>

<success_criteria>
- LocationRef model in base.py with page, region_id, char_offset, char_length
- 8 enums in labels.py with correct value counts (21, 9, 9, 6, 5, 13, 7, 5)
- SFP-3 (ScriptVariant), SFP-4 (COLOR), SFP-5 (INDEX_AREA), SFP-6 (CATCHWORD) all present
- Old enums (ScanQuality, Difficulty, CitationType, MarginalRefType) removed
- DocumentType renamed to DocumentSectionType
- Schema version 2.0.0
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-schema-taxonomy-review-revision/01.1-01-SUMMARY.md`
</output>
