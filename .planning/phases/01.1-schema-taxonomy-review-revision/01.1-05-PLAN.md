---
phase: 01.1-schema-taxonomy-review-revision
plan: 05
type: execute
wave: 5
depends_on:
  - "01.1-04"
files_modified:
  - tests/test_scholargt/test_schema_base.py
  - tests/test_scholargt/test_semantic_models.py
  - tests/test_scholargt/test_page_models.py
  - tests/test_scholargt/test_document_models.py
  - tests/test_scholargt/test_config.py
  - tests/test_scholargt/test_validation.py
  - tests/unit/test_profiles.py
  - docs/gt/examples/extraction-eval-page.json
  - docs/gt/examples/full-scholarly-document.json
  - docs/gt/examples/layout-annotation-page.json
  - scholargt/generated/schema.json
  - docs/gt/SCHEMA_GUIDE.md
autonomous: true
requirements:
  - SCH-01
  - SCH-02

must_haves:
  truths:
    - "All tests pass with uv run pytest (zero failures)"
    - "Tests cover LocationRef, Note (not Footnote/Endnote), Commentary, NoteSchema, LayoutRegister, ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType"
    - "Tests cover SFP additions: register_id on Region, text_direction on Region, base_direction on PageGT, script_variant on FormattingAnnotation, color_value/color_semantic on FormattingAnnotation, INDEX_AREA in SpatialLabel, CATCHWORD in ReferenceSystem"
    - "Tests verify PageQuality hybrid model (categorical + numeric + artifacts)"
    - "Tests verify PageDependency and SectionContextEntry on PageGT"
    - "Example GT JSON files use v2.0.0 models and demonstrate new features"
    - "Generated schema.json reflects v2.0.0 models"
    - "SCHEMA_GUIDE.md documents v2.0.0 changes including SFP features"
    - "uv run ruff check passes with no errors"
  artifacts:
    - path: "tests/test_scholargt/test_schema_base.py"
      provides: "Tests for LocationRef and base models"
      contains: "LocationRef"
    - path: "tests/test_scholargt/test_semantic_models.py"
      provides: "Tests for Note, Commentary, Citation, BibEntry with new fields"
      contains: "class.*Note"
    - path: "scholargt/generated/schema.json"
      provides: "Regenerated JSON Schema for v2.0.0"
      contains: "LayoutRegister"
    - path: "docs/gt/SCHEMA_GUIDE.md"
      provides: "Updated documentation for v2.0.0 schema"
      contains: "register"
  key_links:
    - from: "tests/test_scholargt/test_semantic_models.py"
      to: "scholargt/schema/semantic.py"
      via: "tests construct and validate Note, Commentary, NoteSchema"
      pattern: "Note\\(|Commentary\\("
    - from: "tests/test_scholargt/test_document_models.py"
      to: "scholargt/schema/document.py"
      via: "tests construct DocumentGT with registers, note_schemas"
      pattern: "LayoutRegister\\("
    - from: "docs/gt/examples/full-scholarly-document.json"
      to: "scholargt/schema/document.py"
      via: "example JSON validates against DocumentGT model"
      pattern: "registers"
---

<objective>
Verification: Update all tests for v2.0.0 models, update example GT files to demonstrate new features (including SFPs), regenerate schema.json, and update SCHEMA_GUIDE.md documentation.

Purpose: This plan ensures the schema is not just defined but verified, documented, and demonstrated. All 225+ existing tests must be updated to pass. New tests must cover every new model and SFP feature. Example files must show real-world usage. Documentation must guide annotators and extractor developers.

Output: Passing test suite, valid example files, regenerated JSON Schema, updated documentation.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-CONTEXT.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-RESEARCH.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-03-SUMMARY.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-04-SUMMARY.md
@tests/test_scholargt/test_schema_base.py
@tests/test_scholargt/test_semantic_models.py
@tests/test_scholargt/test_page_models.py
@tests/test_scholargt/test_document_models.py
@tests/test_scholargt/test_config.py
@tests/test_scholargt/test_validation.py
@tests/unit/test_profiles.py
@docs/gt/examples/extraction-eval-page.json
@docs/gt/examples/full-scholarly-document.json
@docs/gt/examples/layout-annotation-page.json
@scholargt/validation/schema_gen.py
@docs/gt/SCHEMA_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all test files for v2.0.0 models</name>
  <files>
tests/test_scholargt/test_schema_base.py
tests/test_scholargt/test_semantic_models.py
tests/test_scholargt/test_page_models.py
tests/test_scholargt/test_document_models.py
tests/test_scholargt/test_config.py
tests/test_scholargt/test_validation.py
tests/unit/test_profiles.py
  </files>
  <action>
Read ALL test files first to understand the existing test structure and patterns. Then update each:

**Strategy:** Use find-and-replace for straightforward renames first, then manual edits for structural changes. Work file-by-file.

**test_schema_base.py:**
- Add tests for LocationRef: construction with all fields, construction with only required fields (page, region_id), JSON roundtrip, validation (no special validators but ensure defaults work)

**test_semantic_models.py (HEAVIEST changes):**
- Replace ALL Footnote test data/constructions with Note equivalents:
  - `element_type="footnote"` -> `element_type="note"`
  - `marker=MarkerInfo(...)` -> `body_marker=LocationRef(...)`
  - `location="page_bottom"` -> `placement="page_bottom"`
  - Add `content_marker` tests
- Replace ALL Endnote test data/constructions with Note equivalents:
  - `element_type="endnote"` -> `element_type="note"` with `placement="end_of_book"`
- Replace CitationType references with CitationFormat + ReferenceSystem:
  - `citation_type=CitationType.AUTHOR_DATE` -> `citation_format=CitationFormat.PARENTHETICAL, reference_system=ReferenceSystem.STANDARD`
  - `citation_type=CitationType.STEPHANUS` -> `citation_format=CitationFormat.PARENTHETICAL, reference_system=ReferenceSystem.STEPHANUS`
- Replace MarginalRefType with ReferenceSystem:
  - `ref_type=MarginalRefType.STEPHANUS` -> `reference_system=ReferenceSystem.STEPHANUS`
- Add new tests for:
  - Note model with all fields (body_marker, content_marker, content, placement, scope, note_source, marker_text, note_schema_id)
  - Commentary model (source, passage_ref, reference_system=CATCHWORD, target_location, content, layer)
  - NoteSchema model (schema_id, marker_type, symbol_sequence, reset_boundary, placement, note_source)
  - BibliographicRecord (comprehensive field test)
  - ContentSpan with char_offset/char_length
  - SemanticElement union deserialization with Note and Commentary
- Remove any tests for Footnote, Endnote, MarkerInfo classes

**test_page_models.py:**
- Update PageQuality tests: replace `scan_quality`/`difficulty` with hybrid model fields (overall, is_scan, artifacts, difficulty_factors, numeric metrics)
- Add tests for PageDependency (all fields)
- Add tests for SectionContextEntry (all fields)
- Add tests for PageGT.section_context, PageGT.page_dependency, PageGT.base_direction (SFP-2)
- Update any Region construction to use new SpatialLabel values (note_area not footnote_area)

**test_document_models.py:**
- Remove tests for FootnoteLink, CitationBibLink, DocumentRelationships
- Add tests for LayoutRegister (all fields including SFP-1 specifics)
- Add tests for DocumentGT.registers, DocumentGT.note_schemas, DocumentGT.citation_style
- Update SemanticElement in DocumentGT.elements to use Note/Commentary instead of Footnote/Endnote
- Test NoteSchema uniqueness validator on DocumentGT

**test_config.py / test_profiles.py:**
- Update GTProfile tests for new categories (citation_formats, reference_systems, note_placements, script_variants)
- Update is_label_enabled tests for new category names
- Update any hardcoded label values (footnote_area -> note_area, footnote -> note)
- Test profile loading with v2.0.0 YAML files

**test_validation.py:**
- Update validation tests to use v2.0.0 models
- Add test for register_id cross-referencing warning
- Add test for COLOR formatting validation warning
- Remove any tests for old relationship models

**SFP-specific test additions across files:**
- Region with register_id="rashi" and text_direction="rtl" (SFP-1, SFP-2)
- FormattingAnnotation with script_variant=ScriptVariant.RASHI_SCRIPT (SFP-3)
- FormattingAnnotation with formatting_type=COLOR, color_value="#FF0000", color_semantic="gemara_text" (SFP-4)
- Region with label=SpatialLabel.INDEX_AREA (SFP-5)
- Commentary with reference_system=ReferenceSystem.CATCHWORD (SFP-6)
- PageGT with base_direction="rtl" (SFP-2)
- DocumentGT with registers=[LayoutRegister(...)] (SFP-1)

After updating all test files, run: `cd /home/rookslog/workspace/projects/scholardoc && uv run pytest tests/test_scholargt/ -x --tb=short` and fix any remaining failures iteratively until all pass.

Then run: `cd /home/rookslog/workspace/projects/scholardoc && uv run pytest tests/ -x --tb=short` to catch integration test failures too. Fix those as well.

Then run: `cd /home/rookslog/workspace/projects/scholardoc && uv run ruff check .` and fix any lint issues.
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run pytest tests/ -v --tb=short 2>&1 | tail -20`
All tests must pass (zero failures, zero errors).

Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run ruff check .`
No lint errors.
  </verify>
  <done>All tests pass (zero failures). Tests cover LocationRef, Note, Commentary, NoteSchema, LayoutRegister, ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType, and all SFP features. No references to deleted models (Footnote, Endnote, FootnoteLink, etc.) in test code. Ruff lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update example GT files and regenerate schema.json</name>
  <files>
docs/gt/examples/extraction-eval-page.json
docs/gt/examples/full-scholarly-document.json
docs/gt/examples/layout-annotation-page.json
scholargt/generated/schema.json
  </files>
  <action>
Read all 3 example JSON files and the schema_gen.py script first.

**Update example JSON files:**

**extraction-eval-page.json (page GT example):**
- Update schema_version to "2.0.0"
- Replace "footnote_area" with "note_area" in region labels
- Replace "footnote_continuation" with "note_continuation" if present
- Add example of base_direction: "ltr" at page level
- Add section_context with at least one SectionContextEntry
- Add page_dependency with continues_from_previous: false, continues_to_next: false
- Add example Region with register_id (SFP-1) and text_direction (SFP-2)
- Keep realistic: this should look like a page from a scholarly text

**full-scholarly-document.json (document GT example):**
- Update schema_version to "2.0.0"
- Replace Footnote elements with Note elements (element_type: "note", body_marker as LocationRef, placement, etc.)
- Replace Endnote elements with Note elements (placement: "end_of_book")
- Replace CitationType with citation_format + reference_system on Citation elements
- Replace ref_type with reference_system on MarginalReference elements
- Remove "relationships" section entirely
- Add note_schemas array with at least one NoteSchema
- Add citation_style (e.g., "chicago_nb")
- Add registers array with at least one LayoutRegister demonstrating SFP-1
- Add a Commentary element demonstrating reference_system: "catchword" (SFP-6)
- Add FormattingAnnotation examples with:
  - language: "de" for a German passage
  - script_variant: "rashi_script" for a Rashi script annotation (SFP-3)
  - formatting_type: "color", color_value: "#0000FF", color_semantic: "mishnah_text" (SFP-4)
- Keep realistic and internally consistent (region_ids match, page indices valid)

**layout-annotation-page.json (page GT example):**
- Update schema_version to "2.0.0"
- Replace "footnote_area" with "note_area"
- Add INDEX_AREA example region (SFP-5)
- Add base_direction field
- Keep minimal (layout-only, no semantic annotations)

**Regenerate schema.json:**
Read `scholargt/validation/schema_gen.py` to understand the generation approach. Run the schema generation:
`cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "from scholargt.validation.schema_gen import generate_schema; generate_schema()"`

Or if the generation script works differently, read and follow its pattern. The output should go to `scholargt/generated/schema.json`. Verify the generated schema contains new model definitions (LayoutRegister, Note, Commentary, ScriptVariant, etc.).

After generation, validate each example against the generated schema:
`cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
import json
from jsonschema import validate
schema = json.load(open('scholargt/generated/schema.json'))
# Validate each example
for f in ['docs/gt/examples/extraction-eval-page.json', 'docs/gt/examples/layout-annotation-page.json']:
    data = json.load(open(f))
    # Page examples validate against PageGT subschema if structure supports it
    print(f'{f}: loaded OK')
for f in ['docs/gt/examples/full-scholarly-document.json']:
    data = json.load(open(f))
    print(f'{f}: loaded OK')
"`
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
import json
# Check schema.json has new models
schema = json.load(open('scholargt/generated/schema.json'))
schema_str = json.dumps(schema)
assert 'LayoutRegister' in schema_str, 'LayoutRegister missing from schema.json'
assert 'ScriptVariant' in schema_str, 'ScriptVariant missing from schema.json'
assert 'NoteSchema' in schema_str, 'NoteSchema missing from schema.json'
# Check examples are valid JSON
for f in ['docs/gt/examples/extraction-eval-page.json', 'docs/gt/examples/full-scholarly-document.json', 'docs/gt/examples/layout-annotation-page.json']:
    data = json.load(open(f))
    assert data.get('schema_version') == '2.0.0', f'{f}: schema_version not 2.0.0'
print('Schema and examples OK')
"
`
  </verify>
  <done>All 3 example JSON files use v2.0.0 models and demonstrate new features (registers, note models, commentary, color annotation, script variant, index area). schema.json regenerated with all new model definitions.</done>
</task>

<task type="auto">
  <name>Task 3: Update SCHEMA_GUIDE.md for v2.0.0</name>
  <files>docs/gt/SCHEMA_GUIDE.md</files>
  <action>
Read the current SCHEMA_GUIDE.md first. Then update it comprehensively for v2.0.0:

**Key sections to update/add:**

1. **Version header:** Note this is v2.0.0, a major breaking change from v1.0.0.

2. **What's new in v2.0.0:** Summary section listing all major changes:
   - Note consolidation (Footnote + Endnote -> Note)
   - Commentary apparatus model
   - CitationType decomposition (CitationFormat x ReferenceSystem x CitationStyle)
   - ScanQuality/Difficulty -> hybrid PageQuality
   - Cross-page relationships (PageDependency, Region continuation flags)
   - Self-describing pages (SectionContextEntry)
   - DocumentRelationships removed (embedded references)
   - LocationRef standardization
   - LayoutRegister for multi-register documents (SFP-1)
   - Explicit text direction (SFP-2)
   - ScriptVariant enum for Rashi/square Hebrew (SFP-3)
   - COLOR formatting with semantic meaning (SFP-4)
   - INDEX_AREA spatial label (SFP-5)
   - CATCHWORD reference system (SFP-6)

3. **Label taxonomy:** Update all enum value tables. Add new enums (ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle). Remove old enums (ScanQuality, Difficulty, CitationType, MarginalRefType). Rename DocumentType to DocumentSectionType.

4. **Note model:** Explain the consolidation rationale, marker locations (body_marker, content_marker), placement/scope/note_source fields, NoteSchema for document-level conventions, how to represent footnotes vs endnotes vs marginal notes using the same model.

5. **Commentary model:** Explain passage_ref based anchoring, reference_system (including CATCHWORD for dibbur ha-matchil), multi-layer support.

6. **Register model:** Explain LayoutRegister (SFP-1) -- what registers are, how Region.register_id links to DocumentGT.registers, examples for Talmudic layout (Gemara + Rashi + Tosafot), Derrida dual-column (Hegel + Genet), bilingual (Hebrew + English).

7. **Text direction:** Explain Region.text_direction (SFP-2), PageGT.base_direction, when to use "bidi", relationship to FormattingAnnotation.language.

8. **Script variants:** Explain ScriptVariant (SFP-3) -- why BCP 47 is insufficient for Rashi/square distinction, when to use script_variant vs language, examples.

9. **Color annotation:** Explain COLOR formatting type (SFP-4) -- color_value (CSS format), color_semantic (domain-specific meaning), validation behavior, examples from Koren Talmud.

10. **PageQuality:** Explain hybrid model replacing ScanQuality/Difficulty enums.

11. **Cross-page relationships:** Explain Region.is_continuation/continues_to_next, PageDependency, SectionContextEntry.

12. **Migration notes:** Brief section noting v1.0.0 files are NOT compatible with v2.0.0. Key renames and removals listed for anyone upgrading.

Keep the guide practical -- aimed at annotators and extractor developers, not schema designers. Use concrete examples where possible.
  </action>
  <verify>
The file should exist and be non-empty:
`ls -la /home/rookslog/workspace/projects/scholardoc/docs/gt/SCHEMA_GUIDE.md`

Verify key sections are present:
`cd /home/rookslog/workspace/projects/scholardoc && grep -c "LayoutRegister\|ScriptVariant\|CATCHWORD\|base_direction\|color_semantic\|INDEX_AREA\|Note.*model\|Commentary.*model\|PageQuality.*hybrid\|PageDependency\|v2.0.0" docs/gt/SCHEMA_GUIDE.md`
Should return a count >= 10, indicating all major topics are covered.
  </verify>
  <done>SCHEMA_GUIDE.md comprehensively documents v2.0.0 changes including all 6 SFP features, Note consolidation, Commentary apparatus, CitationType decomposition, PageQuality hybrid, cross-page relationships, and migration notes.</done>
</task>

</tasks>

<verification>
Final comprehensive verification after all 3 tasks:

1. `cd /home/rookslog/workspace/projects/scholardoc && uv run pytest tests/ -v --tb=short` -- ALL tests pass
2. `cd /home/rookslog/workspace/projects/scholardoc && uv run ruff check .` -- no lint errors
3. `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "from scholargt.schema import *; from scholargt.config.loader import load_profile; p = load_profile('full-scholarly'); print(f'Schema {SCHEMA_VERSION}, {len(p.spatial_labels)} spatial labels, {len(p.reference_systems)} reference systems')"` -- prints "Schema 2.0.0, 21 spatial labels, 13 reference systems"
4. `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "import json; s = json.load(open('scholargt/generated/schema.json')); assert 'LayoutRegister' in json.dumps(s); print('schema.json has v2.0.0 models')"` -- confirms generated schema
</verification>

<success_criteria>
- All tests pass (zero failures, zero errors) via `uv run pytest tests/`
- No lint errors via `uv run ruff check .`
- Tests cover all new models: LocationRef, Note, Commentary, NoteSchema, LayoutRegister, ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType
- Tests cover all SFP features: register_id, text_direction, base_direction, script_variant, color_value/color_semantic, INDEX_AREA, CATCHWORD
- All 3 example JSON files valid with v2.0.0 models and demonstrate new features
- schema.json regenerated with all v2.0.0 model definitions
- SCHEMA_GUIDE.md documents all changes including SFP features
- No references to deleted models (Footnote, Endnote, FootnoteLink, DocumentRelationships, CitationType, MarginalRefType, ScanQuality, Difficulty) anywhere in the codebase except historical documentation
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-schema-taxonomy-review-revision/01.1-05-SUMMARY.md`
</output>
