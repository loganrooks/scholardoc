---
phase: 01.1-schema-taxonomy-review-revision
plan: 05
type: execute
wave: 5
depends_on: [01.1-04]
files_modified:
  - tests/test_scholargt/test_schema_base.py
  - tests/test_scholargt/test_semantic_models.py
  - tests/test_scholargt/test_page_models.py
  - tests/test_scholargt/test_document_models.py
  - tests/test_scholargt/test_config.py
  - tests/test_scholargt/test_validation.py
  - docs/gt/examples/extraction-eval-page.json
  - docs/gt/examples/full-scholarly-document.json
  - docs/gt/examples/layout-annotation-page.json
  - scholargt/generated/schema.json
  - docs/gt/SCHEMA_GUIDE.md
autonomous: true
requirements: [SCH-01 (revised), SCH-02 (revised)]

must_haves:
  truths:
    - "All 6 test files pass with pytest (zero failures)"
    - "test_schema_base.py tests LocationRef creation, serialization, optional fields"
    - "test_semantic_models.py tests Note, Commentary, NoteSchema instead of Footnote/Endnote/MarkerInfo/ParsedCitation"
    - "test_page_models.py tests PageQuality hybrid model, PageDependency, SectionContextEntry, updated PageGT"
    - "test_document_models.py tests DocumentGT with note_schemas and citation_style, no DocumentRelationships"
    - "test_config.py tests profile loading with new label categories (citation_formats, reference_systems, citation_styles)"
    - "test_validation.py tests validator with new model structures (note_schemas checks, no relationship checks)"
    - "Example JSON files use v2.0.0 model structures"
    - "schema.json is regenerated from updated models"
    - "SCHEMA_GUIDE.md documents all new label counts, taxonomy changes, and API references"
  artifacts:
    - path: "tests/test_scholargt/test_schema_base.py"
      provides: "LocationRef tests added"
      contains: "LocationRef"
    - path: "tests/test_scholargt/test_semantic_models.py"
      provides: "Note, Commentary, NoteSchema tests; no Footnote/Endnote tests"
      contains: "class TestNote"
    - path: "tests/test_scholargt/test_page_models.py"
      provides: "PageQuality hybrid, PageDependency, SectionContextEntry tests"
      contains: "class TestPageDependency"
    - path: "tests/test_scholargt/test_document_models.py"
      provides: "DocumentGT tests with note_schemas; no DocumentRelationships tests"
      contains: "note_schemas"
    - path: "tests/test_scholargt/test_config.py"
      provides: "Profile tests with new categories"
      contains: "citation_formats"
    - path: "tests/test_scholargt/test_validation.py"
      provides: "Validator tests with new checks"
      contains: "note_schema_id"
    - path: "docs/gt/examples/full-scholarly-document.json"
      provides: "v2.0.0 document example with Note, citation_format, note_schemas"
      contains: "note_schemas"
    - path: "scholargt/generated/schema.json"
      provides: "Regenerated JSON Schema from v2.0.0 models"
      contains: "2.0.0"
    - path: "docs/gt/SCHEMA_GUIDE.md"
      provides: "Updated documentation for v2.0.0 taxonomy"
      contains: "20"
  key_links:
    - from: "tests/test_scholargt/test_semantic_models.py"
      to: "scholargt/schema/semantic.py"
      via: "Imports and tests Note, Commentary, NoteSchema"
      pattern: "from scholargt.schema.semantic import.*Note.*Commentary"
    - from: "tests/test_scholargt/test_config.py"
      to: "scholargt/config/models.py"
      via: "Tests GTProfile with citation_formats, reference_systems fields"
      pattern: "citation_formats"
    - from: "docs/gt/examples/full-scholarly-document.json"
      to: "scholargt/schema/document.py"
      via: "Example validates against DocumentGT model"
      pattern: "note_schemas"
---

<objective>
Update all tests, example GT files, schema.json, and SCHEMA_GUIDE.md to match the v2.0.0 schema models established by Plans 01-04.

Purpose: The models, re-exports, config, and validation are all updated but the tests still reference old types (Footnote, Endnote, CitationType, DocumentRelationships, etc.), the example JSON files use v1.0.0 structures, schema.json is stale, and SCHEMA_GUIDE.md documents the old taxonomy. This plan brings all verification and documentation artifacts in sync.

Output: All tests passing, 3 updated example JSON files, regenerated schema.json, and updated SCHEMA_GUIDE.md.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-CONTEXT.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-04-SUMMARY.md
@scholargt/schema/base.py
@scholargt/schema/labels.py
@scholargt/schema/semantic.py
@scholargt/schema/spatial.py
@scholargt/schema/formatting.py
@scholargt/schema/page.py
@scholargt/schema/document.py
@scholargt/schema/__init__.py
@scholargt/config/models.py
@tests/test_scholargt/test_schema_base.py
@tests/test_scholargt/test_semantic_models.py
@tests/test_scholargt/test_page_models.py
@tests/test_scholargt/test_document_models.py
@tests/test_scholargt/test_config.py
@tests/test_scholargt/test_validation.py
@docs/gt/examples/extraction-eval-page.json
@docs/gt/examples/full-scholarly-document.json
@docs/gt/examples/layout-annotation-page.json
@docs/gt/SCHEMA_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 6 test files for v2.0.0 models</name>
  <files>tests/test_scholargt/test_schema_base.py, tests/test_scholargt/test_semantic_models.py, tests/test_scholargt/test_page_models.py, tests/test_scholargt/test_document_models.py, tests/test_scholargt/test_config.py, tests/test_scholargt/test_validation.py</files>
  <action>
  Read each test file, understand its test structure, then update to use new models. The approach for each file:

  **test_schema_base.py (270 lines):**
  - ADD tests for `LocationRef`:
    - Test creation with required fields (page, region_id)
    - Test creation with optional fields (char_offset, char_length)
    - Test serialization/deserialization round-trip
    - Test defaults (char_offset=None, char_length=None)
  - KEEP all existing BBox, GTElement, VerificationRecord tests unchanged (these models did not change)
  - Update any version assertions from "1.0.0" to "2.0.0"

  **test_semantic_models.py (604 lines):**
  - REMOVE all tests for `Footnote`, `Endnote`, `MarkerInfo`, `ParsedCitation` — these classes no longer exist
  - ADD tests for `Note`:
    - Test creation with required fields (body_marker as LocationRef, content, placement)
    - Test optional fields (content_marker, scope, note_source, note_schema_id, marker_text)
    - Test element_type is "note"
    - Test serialization round-trip
    - Test placement values ("page_bottom", "end_of_chapter", "end_of_book", "margin")
    - Test scope values ("page", "chapter", "section", "essay", "document")
    - Test note_source values ("author", "translator", "editor")
    - Test discriminated union deserialization (SemanticElement with element_type="note")
  - ADD tests for `Commentary`:
    - Test creation with required fields (source, passage_ref, reference_system, content)
    - Test optional fields (target_location as LocationRef, layer)
    - Test element_type is "commentary"
    - Test discriminated union deserialization
  - ADD tests for `NoteSchema`:
    - Test creation with required fields (schema_id, marker_type, placement)
    - Test optional fields (symbol_sequence, reset_boundary, note_source)
    - Test marker_type values
  - UPDATE `Citation` tests: replace `citation_type` with `citation_format` and `reference_system`; remove `parsed` field tests
  - UPDATE `BibEntry` tests: add tests for new bibliographic fields (title, authors, year, journal, etc.); remove `parsed` field tests
  - UPDATE `MarginalReference` tests: replace `ref_type` with `reference_system`
  - UPDATE `ContentSpan` tests: add tests for new char_offset, char_length fields
  - UPDATE SemanticElement union tests: verify Note and Commentary in the union, verify Footnote/Endnote NOT in union
  - KEEP Section, SousRature, CrossReference, PageNumberAnnotation, ToCEntry tests (these models mostly unchanged)

  **test_page_models.py (301 lines):**
  - UPDATE `PageQuality` tests: replace `scan_quality`/`difficulty` enum-based tests with hybrid model tests (overall categorical, is_scan, artifacts list, numeric metrics, ocr_difficulty, difficulty_factors)
  - ADD tests for `PageDependency`:
    - Test creation with defaults (all False/empty)
    - Test with populated fields
    - Test serialization
  - ADD tests for `SectionContextEntry`:
    - Test creation with required fields (section_id, title, level)
    - Test optional flags (starts_on_this_page, ends_on_this_page)
  - UPDATE `PageGT` tests: add `section_context` and `page_dependency` fields to test instances
  - KEEP Region-related tests that are in page model tests

  **test_document_models.py (632 lines):**
  - REMOVE all tests for `FootnoteLink`, `CitationBibLink`, `DocumentRelationships` — these classes no longer exist
  - REMOVE tests for `DocumentGT.relationships` field
  - ADD tests for `DocumentGT.note_schemas` field (list of NoteSchema)
  - ADD tests for `DocumentGT.citation_style` field (CitationStyle | None)
  - UPDATE DocumentGT construction in tests: remove `relationships` parameter, add `note_schemas` and `citation_style`
  - UPDATE imports: remove old types, add NoteSchema, CitationStyle
  - KEEP DocumentSource, DocumentStructure tests (unchanged)
  - UPDATE any tests that construct semantic elements (Footnote -> Note, Endnote removed)

  **test_config.py (479 lines):**
  - UPDATE profile loading tests to expect new label values:
    - base: `note_area` not `footnote_area`; `note` not `footnote`
    - extraction-eval: `note_area` not `footnote_area`/`endnote_area`; `note` not `footnote`/`endnote`
    - full-scholarly: all 20 spatial labels, 9 semantic types, 8 formatting types; new categories populated
    - layout-annotation: all 20 spatial labels
  - ADD tests for new GTProfile fields: `citation_formats`, `reference_systems`, `citation_styles`, `document_section_types`
  - UPDATE `is_label_enabled` tests: add checks for new categories ("citation_format", "reference_system", "citation_style")
  - UPDATE `enabled_labels` tests: verify 7 categories returned
  - UPDATE `document_types` references to `document_section_types` throughout
  - UPDATE ProjectConfig tests for renamed/new additional fields
  - UPDATE schema_version assertions from "1.0.0" to "2.0.0"

  **test_validation.py (590 lines):**
  - REMOVE tests that validate `relationships`, `footnote_links`, `citation_bib_links` fields
  - ADD tests for new validation checks:
    - note_schema_id reference validation (note referencing unknown schema_id should warn)
    - bib_entry_id reference validation (citation referencing unknown bib_entry_id should warn)
    - citation_format profile check
    - section_context structure check
  - UPDATE all test data dicts to use v2.0.0 structures:
    - Replace `"element_type": "footnote"` with `"element_type": "note"`
    - Replace `"citation_type": "..."` with `"citation_format": "..."`
    - Remove `"relationships"` from document test data
    - Add `"note_schemas"` where appropriate
    - Replace `"scan_quality"` / `"difficulty"` in page quality with hybrid model format
    - Use `"note_area"` instead of `"footnote_area"` in spatial labels
  - UPDATE profile-based validation tests to use new label values
  - KEEP the general validation framework tests (ValidationResult, file loading, auto-detection)

  **General principles for ALL test files:**
  - Update ALL import statements at the top of each file
  - Use `from scholargt.schema.X import Y` or `from scholargt import Y` — both should work after Plan 04
  - Keep the existing test class/method structure where possible — rename and update rather than full rewrite
  - Every test that constructs a model instance must use the new field names
  - Run `uv run pytest tests/test_scholargt/ -x` after ALL changes to verify
  </action>
  <verify>
  ```bash
  uv run pytest tests/test_scholargt/ -x -v --tb=short 2>&1 | tail -40
  ```
  </verify>
  <done>
  All 6 test files updated for v2.0.0 models. No tests reference removed types (Footnote, Endnote, MarkerInfo, ParsedCitation, CitationBibLink, FootnoteLink, DocumentRelationships, CitationType, Difficulty, ScanQuality, MarginalRefType, DocumentType). New tests cover LocationRef, Note, Commentary, NoteSchema, PageDependency, SectionContextEntry, hybrid PageQuality, new GTProfile categories, and updated validator checks. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update example GT files, regenerate schema.json, update SCHEMA_GUIDE.md</name>
  <files>docs/gt/examples/extraction-eval-page.json, docs/gt/examples/full-scholarly-document.json, docs/gt/examples/layout-annotation-page.json, scholargt/generated/schema.json, docs/gt/SCHEMA_GUIDE.md</files>
  <action>
  **Update `docs/gt/examples/extraction-eval-page.json`:**
  - Change `"schema_version": "1.0.0"` to `"schema_version": "2.0.0"`
  - Change region r4 `"label": "footnote_area"` to `"label": "note_area"`
  - Replace `"quality": {"scan_quality": "high", "difficulty": "medium"}` with the hybrid model:
    ```json
    "quality": {
      "overall": "high",
      "is_scan": true,
      "artifacts": [],
      "ocr_difficulty": "medium",
      "difficulty_factors": ["dense_footnotes"]
    }
    ```
  - Keep everything else (text, regions, reading_order, verifications) unchanged

  **Update `docs/gt/examples/layout-annotation-page.json`:**
  - Change `"schema_version": "1.0.0"` to `"schema_version": "2.0.0"`
  - Change region r6 `"label": "footnote_area"` to `"label": "note_area"`
  - Replace `"quality": {"scan_quality": "medium", "difficulty": "easy"}` with:
    ```json
    "quality": {
      "overall": "medium",
      "is_scan": true,
      "artifacts": [],
      "ocr_difficulty": "low",
      "difficulty_factors": []
    }
    ```
  - Keep everything else unchanged

  **Update `docs/gt/examples/full-scholarly-document.json`:**
  This is the most complex update. Changes:

  - Change `"schema_version": "1.0.0"` to `"schema_version": "2.0.0"`

  - Convert the footnote element (id: "fn-1-p21") from `"element_type": "footnote"` to `"element_type": "note"`. Replace the `"marker"` object with `"body_marker"` using LocationRef format:
    ```json
    "body_marker": {
      "page": 21,
      "region_id": "r2",
      "char_offset": 342
    }
    ```
  - Remove `"location": "page_bottom"` and add `"placement": "page_bottom"` on the note
  - Add `"marker_text": "1"` field
  - Keep `"note_source": "translator"` and `"content"` array as-is (ContentSpan format unchanged)

  - Update the citation element (id: "cit-sophist-244a"):
    - Replace `"citation_type": "stephanus"` with:
      ```json
      "citation_format": "inline_author",
      "reference_system": "stephanus"
      ```
    - Remove `"parsed"` object entirely (ParsedCitation removed)
    - Keep `"raw_text"`, `"bib_entry_id"`, `"page"`, `"region_id"`

  - Update the bib entry (id: "bib-plato-sophist"):
    - Remove `"parsed"` object
    - Add expanded bibliographic fields:
      ```json
      "title": "Sophist",
      "authors": ["Plato"],
      "year": "1997",
      "publisher": "Hackett"
      ```
    - Keep `"raw_text"`, `"entry_index"`

  - REMOVE the entire `"relationships"` block (footnote_links, citation_bib_links, cross_refs)

  - ADD `"note_schemas"` after `"formatting"`:
    ```json
    "note_schemas": [
      {
        "schema_id": "translator-footnotes",
        "marker_type": "arabic",
        "placement": "page_bottom",
        "note_source": "translator"
      }
    ]
    ```

  - ADD `"citation_style": "chicago_nb"` after `"note_schemas"`

  - Keep all other fields unchanged (source, page_range, sections, sous_rature, formatting, structure, config_profile, verifications)

  **Regenerate `scholargt/generated/schema.json`:**
  ```bash
  uv run python -c "from scholargt.validation import write_schema; write_schema()"
  ```

  **Update `docs/gt/SCHEMA_GUIDE.md`:**

  Major updates needed throughout:

  1. **Schema Architecture section**: Replace "Relationships: Footnote-to-content links, citation-to-bibliography links" with "Note schemas: Document-level note numbering descriptions" and "Relationships are embedded in elements (Note markers, Citation bib_entry_id)"

  2. **Label Taxonomy section**: Update to "7 independent dimensions" (was 5). Update all tables:

  - **Spatial Labels**: Change heading to "(20)" from "(17)". Replace `footnote_area`/`endnote_area`/`footnote_continuation` with `note_area`/`note_continuation`. Add `toc_area`, `abstract`, `code_block`, `unknown`. Update descriptions.

  - **Semantic Types**: Keep "(9)" heading. Replace `footnote`/`endnote` with `note`. Add `commentary`. Update descriptions.

  - **Formatting Types**: Change to "(8)" from "(6)". Add `subscript`, `monospace`.

  - **Document Section Types**: Rename from "Document Types". Keep "(5)".

  - ADD **Citation Formats (5)** section:
    | Format | Description |
    | `parenthetical` | Parenthetical citation (Smith, 2020) |
    | `numeric` | Numeric reference [1] or superscript |
    | `inline_author` | Inline author reference "Smith argues..." |
    | `note_based` | Citation as footnote (Chicago NB) |
    | `author_title` | Author + short title reference |

  - ADD **Reference Systems (12)** section:
    | System | Description |
    | `standard` | Author + Year + Page |
    | `stephanus` | Platonic dialogue pagination |
    | `bekker` | Aristotelian page + column + line |
    | `akademie` | Kantian Akademie edition |
    | `ab_edition` | Kant KrV A/B edition pages |
    | `paragraph` | Section/paragraph numbers (Hegel) |
    | `sz_pagination` | Heidegger SZ page numbers |
    | `diels_kranz` | Pre-Socratic fragments |
    | `line_number` | Line/verse numbers |
    | `chapter_verse` | Biblical chapter:verse |
    | `legal` | Legal citation format |
    | `custom` | Project-specific system |

  - ADD **Citation Styles (7)** section:
    | Style | Description |
    | `apa` | American Psychological Association |
    | `chicago_nb` | Chicago Notes-Bibliography |
    | `chicago_ad` | Chicago Author-Date |
    | `mla` | Modern Language Association |
    | `vancouver` | Vancouver (numbered, medical/science) |
    | `turabian` | Turabian (Chicago variant) |
    | `custom` | Project-specific style |

  - **Quality Labels section**: Replace scan_quality/difficulty enum description with hybrid PageQuality description:
    - `overall`: categorical (low/medium/high)
    - `is_scan`: boolean
    - `artifacts`: list of specific issues
    - Numeric metrics: dpi_estimate, contrast_ratio, skew_angle, noise_level
    - `ocr_difficulty`: categorical + `difficulty_factors` list

  3. **Configuration Profiles section**: Update the profile summary table:
    | Profile | Spatial | Semantic | Formatting | DocSection | CitFmt | RefSys | CitStyle | Key Validation |
    | base | 6 | 2 | 0 | 1 | 0 | 0 | 0 | require_bbox |
    | extraction-eval | 8 | 4 | 0 | 1 | 0 | 0 | 0 | require_text, require_reading_order |
    | layout-annotation | 20 | 0 | 0 | 1 | 0 | 0 | 0 | require_bbox, require_reading_order |
    | full-scholarly | 20 | 9 | 8 | 5 | 5 | 12 | 7 | All requirements, confidence=0.9 |

  4. **Example Walkthroughs section**: Update descriptions to match the new example files:
    - extraction-eval-page: mention hybrid quality, note_area label
    - layout-annotation-page: mention all 20 spatial label types, hybrid quality
    - full-scholarly-document: mention Note (not Footnote), citation_format+reference_system, note_schemas, no relationships block, citation_style

  5. **API Reference section**: Update imports:
    ```python
    from scholargt import (
        # Core models
        PageGT, DocumentGT, Region, BBox, GTElement, LocationRef,
        # Semantic elements
        Note, Commentary, Citation, Section, BibEntry, SousRature, NoteSchema,
        # Labels
        SpatialLabel, SemanticType, CitationFormat, ReferenceSystem, CitationStyle,
        # Config
        GTProfile, load_profile, list_profiles,
        # Validation
        validate_gt_file, validate_page_gt, validate_document_gt,
        ValidationResult, generate_schema, write_schema,
    )
    ```

  6. **Multi-dimensional Labeling section**: Update the semantic_labels reference from "footnote" to "note"

  7. **Cross-page Relationships section** (NEW): Add a section explaining:
    - Region-level: is_continuation, continues_to_next flags
    - Page-level: PageDependency with continues_from/to, unresolved_markers, orphan_continuations
    - Self-describing pages: SectionContextEntry on PageGT

  8. **Note Consolidation section** (NEW): Brief explanation that Note replaces Footnote+Endnote with differentiating properties (placement, scope, note_source). NoteSchema at document level.
  </action>
  <verify>
  ```bash
  # Verify examples validate against updated models
  uv run python -c "
from scholargt.validation import validate_gt_file
from pathlib import Path

examples_dir = Path('docs/gt/examples')
for f in sorted(examples_dir.glob('*.json')):
    result = validate_gt_file(f)
    status = 'PASS' if result.valid else 'FAIL'
    print(f'{f.name}: {status} (errors={len(result.errors)}, warnings={len(result.warnings)})')
    for e in result.errors:
        print(f'  ERROR: {e}')
print()

# Verify schema.json was regenerated
import json
schema_path = Path('scholargt/generated/schema.json')
assert schema_path.exists(), 'schema.json not found'
schema = json.loads(schema_path.read_text())
assert schema.get('version') == '2.0.0', f'Expected version 2.0.0, got {schema.get(\"version\")}'
defs = schema.get('\$defs', {})
assert 'Note' in defs or 'note' in str(defs).lower(), 'Note model should be in schema defs'
print(f'schema.json: version={schema[\"version\"]}, defs={len(defs)}')
print('Examples and schema OK')
"

  # Verify SCHEMA_GUIDE.md was updated
  grep -c "note_area" docs/gt/SCHEMA_GUIDE.md && echo "SCHEMA_GUIDE updated"

  # Run full test suite
  uv run pytest tests/test_scholargt/ -v --tb=short 2>&1 | tail -50
  ```
  </verify>
  <done>
  All 6 test files pass. Three example JSON files updated to v2.0.0 structures (Note instead of Footnote, citation_format instead of citation_type, hybrid PageQuality, note_schemas, no relationships). schema.json regenerated. SCHEMA_GUIDE.md documents all 7 label categories, new models, cross-page relationships, and updated API reference.
  </done>
</task>

</tasks>

<verification>
```bash
# Final comprehensive verification
uv run python -c "
# 1. Full import chain works
from scholargt import (
    PageGT, DocumentGT, Region, BBox, GTElement, LocationRef,
    Note, Commentary, NoteSchema, Citation, BibEntry, Section,
    SousRature, CrossReference, MarginalReference, PageNumberAnnotation,
    ContentSpan, ToCEntry, SemanticElement,
    CitationFormat, ReferenceSystem, CitationStyle,
    SpatialLabel, SemanticType, FormattingType, DocumentSectionType,
    PageQuality, PageDependency, SectionContextEntry,
    FormattingAnnotation, DocumentSource, DocumentStructure,
    VerificationRecord, SCHEMA_VERSION,
    GTProfile, load_profile, list_profiles,
    validate_gt_file, validate_page_gt, validate_document_gt,
    ValidationResult, generate_schema, write_schema,
)
assert SCHEMA_VERSION == '2.0.0'
print('1. All imports OK, version 2.0.0')

# 2. Label counts
assert len(SpatialLabel) == 20
assert len(SemanticType) == 9
assert len(FormattingType) == 8
assert len(DocumentSectionType) == 5
assert len(CitationFormat) == 5
assert len(ReferenceSystem) == 12
assert len(CitationStyle) == 7
print('2. Label counts: 20/9/8/5/5/12/7 OK')

# 3. Profile loads
profile = load_profile('full-scholarly')
assert len(profile.spatial_labels) == 20
assert len(profile.semantic_types) == 9
assert len(profile.citation_formats) == 5
assert len(profile.reference_systems) == 12
assert len(profile.citation_styles) == 7
print('3. Full-scholarly profile OK')

# 4. Example files validate
from pathlib import Path
for f in sorted(Path('docs/gt/examples').glob('*.json')):
    r = validate_gt_file(f)
    assert r.valid or len(r.errors) == 0, f'{f.name} has errors: {r.errors}'
print('4. Example files validate OK')

# 5. Schema.json exists and is v2
import json
schema = json.loads(Path('scholargt/generated/schema.json').read_text())
assert schema['version'] == '2.0.0'
print('5. schema.json v2.0.0 OK')

print()
print('ALL VERIFICATION CHECKS PASSED')
"

# 6. Full test suite
uv run pytest tests/test_scholargt/ -v --tb=short
```
</verification>

<success_criteria>
1. All 6 test files pass (zero failures) with `uv run pytest tests/test_scholargt/`
2. Tests cover all new models: LocationRef, Note, Commentary, NoteSchema, PageDependency, SectionContextEntry, hybrid PageQuality
3. Tests do NOT reference removed models: Footnote, Endnote, MarkerInfo, ParsedCitation, CitationBibLink, FootnoteLink, DocumentRelationships, CitationType, ScanQuality, Difficulty, MarginalRefType
4. All 3 example JSON files use v2.0.0 structures and validate without errors
5. schema.json is regenerated with version 2.0.0
6. SCHEMA_GUIDE.md documents all 7 label categories with correct counts (20/9/8/5/5/12/7)
7. `uv run ruff check .` passes (no lint errors introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-schema-taxonomy-review-revision/01.1-05-SUMMARY.md`
</output>
