---
phase: 01.1-schema-taxonomy-review-revision
plan: 02
subsystem: schema
tags: [pydantic, models, Region, Note, Commentary, FormattingAnnotation, SFP-1, SFP-2, SFP-3, SFP-4, LocationRef]

# Dependency graph
requires:
  - phase: 01.1-schema-taxonomy-review-revision
    provides: "LocationRef model, 8 reorganized label enums (Plan 01)"
provides:
  - "Region model with continuation, sub-regions, register_id (SFP-1), text_direction (SFP-2)"
  - "Note model replacing Footnote+Endnote with LocationRef-based markers"
  - "Commentary model for philosophical/rabbinic commentary apparatus"
  - "NoteSchema model for document-level note numbering conventions"
  - "BibliographicRecord for full bibliography fields on BibEntry"
  - "Updated Citation with CitationFormat + ReferenceSystem decomposition"
  - "Updated MarginalReference with shared ReferenceSystem"
  - "FormattingAnnotation with language, script_variant (SFP-3), color fields (SFP-4)"
  - "SemanticElement union including Note, Commentary; excluding Footnote, Endnote"
affects: [01.1-03, 01.1-04, 01.1-05, page, document, config, __init__]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "LocationRef as marker reference (body_marker, content_marker, target_location)"
    - "Unified Note model with placement discriminator (page_bottom, end_of_chapter, end_of_book, margin)"
    - "Two-dimension citation decomposition: format (appearance) vs reference_system (coordinates)"
    - "model_validator warnings (not errors) for incremental annotation support"
    - "Sub-region hierarchy via self-referencing children list"

key-files:
  created: []
  modified:
    - "scholargt/schema/spatial.py"
    - "scholargt/schema/semantic.py"
    - "scholargt/schema/formatting.py"

key-decisions:
  - "Note unifies Footnote+Endnote via placement field (page_bottom vs end_of_chapter vs end_of_book vs margin)"
  - "Commentary uses ReferenceSystem for passage location (shared with Citation and MarginalReference)"
  - "BibEntry.record replaces BibEntry.parsed (BibliographicRecord vs ParsedCitation) -- richer fields for bibliography"
  - "FormattingAnnotation color validator warns rather than errors (supports incremental annotation)"
  - "Region.children is list[Region] with forward reference via from __future__ import annotations"

patterns-established:
  - "LocationRef usage: Note.body_marker, Note.content_marker, Commentary.target_location"
  - "Placement-based note discrimination instead of separate Footnote/Endnote classes"
  - "NoteSchema as document-level metadata (BaseModel, not GTElement)"
  - "Color annotation: formatting_type=COLOR + color_value + color_semantic triple"

requirements-completed: [SCH-01]

# Metrics
duration: 3min
completed: 2026-02-20
---

# Phase 1.1 Plan 02: Core Models Summary

**Region with continuation/hierarchy/SFP fields, unified Note replacing Footnote+Endnote, Commentary apparatus, decomposed Citation, and FormattingAnnotation with language/script/color**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-20T01:02:19Z
- **Completed:** 2026-02-20T01:05:37Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Region model extended with 6 new fields: cross-page continuation (is_continuation, continues_to_next), semantic back-references, sub-region children, register_id (SFP-1), and text_direction (SFP-2)
- semantic.py completely rewritten: Note replaces Footnote+Endnote using LocationRef markers, Commentary model added for philosophical/rabbinic apparatus, Citation decomposed into CitationFormat + ReferenceSystem, BibEntry upgraded to BibliographicRecord, NoteSchema added for numbering conventions
- FormattingAnnotation extended with 4 new fields: language (BCP 47), script_variant (SFP-3), color_value and color_semantic (SFP-4) with model_validator warnings

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite Region model in spatial.py** - `69adcc1` (feat)
2. **Task 2: Rewrite semantic models in semantic.py** - `9d8a962` (feat)
3. **Task 3: Rewrite FormattingAnnotation in formatting.py** - `648cfb4` (feat)

## Files Created/Modified
- `scholargt/schema/spatial.py` - Region model with continuation, hierarchy, register_id (SFP-1), text_direction (SFP-2)
- `scholargt/schema/semantic.py` - Complete rewrite: Note, Commentary, NoteSchema, BibliographicRecord, updated Citation/BibEntry/MarginalReference, SemanticElement union
- `scholargt/schema/formatting.py` - FormattingAnnotation with language, script_variant (SFP-3), color_value/color_semantic (SFP-4), model_validator warnings

## Decisions Made
- Note unifies Footnote+Endnote via placement field rather than separate classes -- simpler model, same expressiveness
- Commentary uses shared ReferenceSystem enum for passage location (consistent with Citation, MarginalReference)
- BibEntry.record (BibliographicRecord) replaces BibEntry.parsed (ParsedCitation) -- bibliography needs richer fields than in-text citation parsing
- FormattingAnnotation color validator issues warnings not errors -- annotation may be incremental, color_value added in later pass
- Region.children uses self-referencing list[Region] with forward reference for sub-region nesting

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Package-level import chain broken because __init__.py still imports old models (Footnote, Endnote, FootnoteLink). Verification used direct importlib module loading to bypass broken chain. This is expected and documented -- Plan 03 fixes page.py/document.py, Plan 04 fixes __init__.py.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All 3 core model files (spatial, semantic, formatting) rewritten with v2.0.0 types
- Ready for Plan 03 (page.py and document.py container models rewrite)
- Import chain still broken until Plan 03-04 update page.py, document.py, __init__.py
- Tests will fail until Plan 05 (test suite update)

## Self-Check: PASSED

All files verified present. All commit hashes verified in git log.

---
*Phase: 01.1-schema-taxonomy-review-revision*
*Completed: 2026-02-20*
