---
phase: 01.1-schema-taxonomy-review-revision
plan: 04
type: execute
wave: 4
depends_on:
  - "01.1-03"
files_modified:
  - scholargt/schema/__init__.py
  - scholargt/config/models.py
  - scholargt/config/profiles/base.yaml
  - scholargt/config/profiles/extraction-eval.yaml
  - scholargt/config/profiles/layout-annotation.yaml
  - scholargt/config/profiles/full-scholarly.yaml
  - scholargt/validation/validator.py
autonomous: true
requirements:
  - SCH-02

must_haves:
  truths:
    - "__init__.py re-exports all new models (LocationRef, Note, Commentary, NoteSchema, LayoutRegister, ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType) and removes old ones (Footnote, Endnote, FootnoteLink, CitationBibLink, DocumentRelationships, CitationType, MarginalRefType, ScanQuality, Difficulty)"
    - "GTProfile has new label categories: citation_formats, reference_systems, note_placements, script_variants"
    - "All 4 YAML profiles updated with new/renamed enum values and new categories"
    - "Validator updated for v2.0.0 models (Note instead of Footnote, register_id cross-referencing warnings)"
  artifacts:
    - path: "scholargt/schema/__init__.py"
      provides: "Clean re-exports for v2.0.0 schema"
      contains: "LayoutRegister"
    - path: "scholargt/config/models.py"
      provides: "GTProfile with new label categories"
      contains: "citation_formats"
    - path: "scholargt/config/profiles/full-scholarly.yaml"
      provides: "Full scholarly profile with all new labels"
      contains: "index_area"
    - path: "scholargt/validation/validator.py"
      provides: "Updated validation rules for v2.0.0"
      contains: "register_id"
  key_links:
    - from: "scholargt/schema/__init__.py"
      to: "scholargt/schema/document.py"
      via: "re-exports LayoutRegister"
      pattern: "from scholargt.schema.document import.*LayoutRegister"
    - from: "scholargt/config/models.py"
      to: "scholargt/schema/labels.py"
      via: "GTProfile categories match label enum values"
      pattern: "citation_formats.*set\\[str\\]"
    - from: "scholargt/validation/validator.py"
      to: "scholargt/schema/semantic.py"
      via: "validates Note model (not Footnote)"
      pattern: "Note|note"
---

<objective>
Integration: Update __init__.py re-exports for v2.0.0, add new label categories to GTProfile, update all 4 YAML profiles with new/renamed enum values, and update validator for new models.

Purpose: This plan makes the schema usable end-to-end. Without updated exports, consumers cannot import the new models. Without updated profiles, config-driven label selection (SCH-02) breaks. Without updated validation, the cross-file validator produces false errors/misses.

Output: Working imports, config-driven profiles supporting all new labels, and validator compatible with v2.0.0 models.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-CONTEXT.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-RESEARCH.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-02-SUMMARY.md
@.planning/phases/01.1-schema-taxonomy-review-revision/01.1-03-SUMMARY.md
@scholargt/schema/__init__.py
@scholargt/config/models.py
@scholargt/config/profiles/base.yaml
@scholargt/config/profiles/extraction-eval.yaml
@scholargt/config/profiles/layout-annotation.yaml
@scholargt/config/profiles/full-scholarly.yaml
@scholargt/validation/validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update __init__.py re-exports for v2.0.0</name>
  <files>scholargt/schema/__init__.py</files>
  <action>
REWRITE `scholargt/schema/__init__.py` to re-export all v2.0.0 models.

**Add imports for new models:**
- From base.py: LocationRef (add to existing BBox, GTElement, VerificationRecord)
- From labels.py: ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType (add to existing SpatialLabel, SemanticType, FormattingType)
- From semantic.py: Note, Commentary, NoteSchema, BibliographicRecord (add to existing); ContentSpan keeps char_offset/char_length
- From document.py: LayoutRegister (add to existing DocumentGT, DocumentSource, DocumentStructure)
- From page.py: PageDependency, SectionContextEntry (add to existing PageGT, PageQuality)

**Remove imports for deleted models:**
- From labels.py: Remove CitationType, MarginalRefType, ScanQuality, Difficulty, DocumentType (renamed to DocumentSectionType)
- From semantic.py: Remove Footnote, Endnote, MarkerInfo (replaced by LocationRef)
- From document.py: Remove FootnoteLink, CitationBibLink, DocumentRelationships

**Update __all__ list** to match the new imports. Organize by section:
- Version
- Base models (BBox, GTElement, VerificationRecord, LocationRef)
- Spatial (Region)
- Page-level (PageGT, PageQuality, PageDependency, SectionContextEntry)
- Semantic elements (Note, Commentary, Citation, BibEntry, Section, SousRature, CrossReference, MarginalReference, PageNumberAnnotation, SemanticElement)
- Supporting models (ContentSpan, ParsedCitation, BibliographicRecord, ToCEntry, NoteSchema)
- Formatting (FormattingAnnotation)
- Document-level (DocumentGT, DocumentSource, DocumentStructure, LayoutRegister)
- Label enums (SpatialLabel, SemanticType, FormattingType, ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType)
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
from scholargt.schema import (
    # New models
    LocationRef, Note, Commentary, NoteSchema, BibliographicRecord,
    LayoutRegister, PageDependency, SectionContextEntry,
    # New enums
    ScriptVariant, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType,
    # Existing
    PageGT, DocumentGT, Region, SemanticElement, FormattingAnnotation,
    BBox, GTElement, VerificationRecord, SpatialLabel, SemanticType, FormattingType,
    ContentSpan, ParsedCitation, ToCEntry, BibEntry, Citation, Section,
    SousRature, CrossReference, MarginalReference, PageNumberAnnotation,
    DocumentSource, DocumentStructure, PageQuality, SCHEMA_VERSION
)
assert SCHEMA_VERSION == '2.0.0'
# Verify old models NOT importable
import scholargt.schema as s
assert not hasattr(s, 'Footnote'), 'Footnote should not be exported'
assert not hasattr(s, 'Endnote'), 'Endnote should not be exported'
assert not hasattr(s, 'FootnoteLink'), 'FootnoteLink should not be exported'
assert not hasattr(s, 'DocumentRelationships'), 'DocumentRelationships should not be exported'
assert not hasattr(s, 'CitationType'), 'CitationType should not be exported'
assert not hasattr(s, 'MarginalRefType'), 'MarginalRefType should not be exported'
assert not hasattr(s, 'ScanQuality'), 'ScanQuality should not be exported'
assert not hasattr(s, 'Difficulty'), 'Difficulty should not be exported'
print('All v2.0.0 exports correct')
"
`
  </verify>
  <done>__init__.py exports all v2.0.0 models and enums, does not export any deleted models. All new models importable via `from scholargt.schema import X`.</done>
</task>

<task type="auto">
  <name>Task 2: Update GTProfile and all YAML profiles for v2.0.0</name>
  <files>
scholargt/config/models.py
scholargt/config/profiles/base.yaml
scholargt/config/profiles/extraction-eval.yaml
scholargt/config/profiles/layout-annotation.yaml
scholargt/config/profiles/full-scholarly.yaml
  </files>
  <action>
**A. Update GTProfile in config/models.py:**

Add new label category fields to GTProfile class:
- `citation_formats: set[str]` (default_factory=set) -- active CitationFormat values
- `reference_systems: set[str]` (default_factory=set) -- active ReferenceSystem values
- `note_placements: set[str]` (default_factory=set) -- active Note.placement values
- `script_variants: set[str]` (default_factory=set) -- active ScriptVariant values

Update `spatial_labels` default to use "note_area" instead of "footnote_area" (reflect v2.0.0 label names).

Update `semantic_types` default to use "note" instead of "footnote" (reflect Note consolidation).

Rename `document_types` field to `document_section_types` (matching DocumentSectionType enum rename).

Update `is_label_enabled()` method's category_map to include new categories: "citation_format", "reference_system", "note_placement", "script_variant". Update "document" key to "document_section".

Update `enabled_labels()` to return all categories including new ones.

Update `ProjectConfig.additional_document_types` to `additional_document_section_types`.

**B. Update all 4 YAML profiles:**

Each YAML file must use v2.0.0 enum values. Key changes across all profiles:

1. `schema_version: "2.0.0"` in all profiles
2. `spatial_labels`: Replace "footnote_area" with "note_area", "footnote_continuation" with "note_continuation", "endnote_area" removed
3. `semantic_types`: Replace "footnote" and "endnote" with "note"; add "commentary" where appropriate
4. `document_types` key renamed to `document_section_types`
5. Add new category sections where appropriate

**base.yaml:**
- spatial_labels: [text_block, note_area, page_header, page_footer, page_number, section_header]
- semantic_types: [note, citation]
- formatting_types: [] (empty -- base is minimal)
- document_section_types: [metadata]
- citation_formats: []
- reference_systems: []
- note_placements: []
- script_variants: []

**extraction-eval.yaml:**
- spatial_labels: all 21 values
- semantic_types: all 9 values
- formatting_types: all 9 values (including color, subscript, monospace)
- document_section_types: all 5
- citation_formats: all 5
- reference_systems: all 13
- note_placements: [page_bottom, end_of_chapter, end_of_book, margin]
- script_variants: all 6

**layout-annotation.yaml:**
- spatial_labels: all 21 values
- semantic_types: [] (layout only -- no semantic annotation)
- formatting_types: [] (layout only)
- document_section_types: []
- citation_formats: []
- reference_systems: []
- note_placements: []
- script_variants: []

**full-scholarly.yaml:**
- spatial_labels: all 21 values
- semantic_types: all 9 values
- formatting_types: all 9 values
- document_section_types: all 5
- citation_formats: all 5
- reference_systems: all 13
- note_placements: [page_bottom, end_of_chapter, end_of_book, margin]
- script_variants: all 6

In each YAML, list all values explicitly (not as references) for clarity.
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
from scholargt.config.models import GTProfile
# Test new category fields exist
p = GTProfile()
assert hasattr(p, 'citation_formats')
assert hasattr(p, 'reference_systems')
assert hasattr(p, 'note_placements')
assert hasattr(p, 'script_variants')
assert hasattr(p, 'document_section_types')
# Test default spatial uses note_area not footnote_area
assert 'note_area' in p.spatial_labels
assert 'footnote_area' not in p.spatial_labels
# Test is_label_enabled with new categories
p2 = GTProfile(citation_formats={'parenthetical', 'numeric'})
assert p2.is_label_enabled('citation_format', 'parenthetical')
assert not p2.is_label_enabled('citation_format', 'note_based')
print('GTProfile checks passed')
"
`

Then verify YAML profile loading:
`cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
from scholargt.config.loader import load_profile
p = load_profile('full-scholarly')
assert 'note_area' in p.spatial_labels
assert 'index_area' in p.spatial_labels
assert 'note' in p.semantic_types
assert 'commentary' in p.semantic_types
assert 'color' in p.formatting_types
assert 'catchword' in p.reference_systems
assert 'rashi_script' in p.script_variants
print('Full scholarly profile loaded with all v2.0.0 values')
"
`
  </verify>
  <done>GTProfile has citation_formats, reference_systems, note_placements, script_variants fields. All 4 YAML profiles use v2.0.0 enum values (note_area not footnote_area, note not footnote, includes COLOR, INDEX_AREA, CATCHWORD, ScriptVariant values). Profile loading works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Update validator for v2.0.0 models</name>
  <files>scholargt/validation/validator.py</files>
  <action>
Read the current `scholargt/validation/validator.py` first. Then update it to work with v2.0.0 models:

1. **Update imports:** Replace old model imports (Footnote, Endnote, FootnoteLink, CitationBibLink, DocumentRelationships, CitationType, MarginalRefType, ScanQuality, Difficulty) with new ones (Note, Commentary, NoteSchema, LayoutRegister, LocationRef, CitationFormat, ReferenceSystem, CitationStyle, DocumentSectionType, ScriptVariant, PageDependency, SectionContextEntry).

2. **Update validation rules:**
   - Replace any Footnote/Endnote validation with Note validation
   - Replace any FootnoteLink/CitationBibLink validation with embedded reference validation (Note.body_marker LocationRef, Citation.bib_entry_id)
   - Add register_id cross-reference validation: warn if any Region.register_id value is not in DocumentGT.registers (only when both page and document GT are available)
   - Add NoteSchema.schema_id uniqueness check
   - Update any ScanQuality/Difficulty checks to use the new PageQuality hybrid model
   - Update any CitationType references to CitationFormat + ReferenceSystem
   - Add validation for COLOR formatting: warn if FormattingAnnotation has formatting_type=COLOR but no color_value

3. **Preserve validation pattern:** Keep the existing error vs warning separation. Keep the existing validation function signatures where possible. The validator should still work with `validate_page(PageGT)` and `validate_document(DocumentGT)` or whatever the current API is.

If the current validator is relatively simple, a full rewrite may be easier than surgical edits. Use judgment based on the existing code complexity.
  </action>
  <verify>
Run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
from scholargt.validation.validator import *
print('Validator imports OK')
"
`

Then run: `cd /home/rookslog/workspace/projects/scholardoc && uv run python -c "
from scholargt.schema import PageGT, Region, Note, LocationRef, ContentSpan
from scholargt.schema.labels import SpatialLabel
# Construct a minimal valid page and verify no crash
page = PageGT(page_index=0, regions=[
    Region(id='r1', label=SpatialLabel.TEXT_BLOCK, bbox={'x0':0,'y0':0,'x1':1,'y1':1})
])
print('Validator compatible with v2.0.0 models')
"
`
  </verify>
  <done>Validator updated for v2.0.0 models. References Note instead of Footnote/Endnote. Validates register_id cross-references, NoteSchema uniqueness, COLOR formatting consistency. No references to deleted models or enums.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `uv run python -c "from scholargt.schema import *; print('Schema imports OK')"` -- all v2.0.0 models importable
2. `uv run python -c "from scholargt.config.loader import load_profile; p = load_profile('full-scholarly'); print(len(p.spatial_labels), 'spatial labels')"` -- profile loading works
3. `uv run python -c "from scholargt.validation.validator import *; print('Validator OK')"` -- validator imports clean

NOTE: Tests will still fail because test files reference old models/enums. Plan 05 fixes all tests.
</verification>

<success_criteria>
- __init__.py re-exports all new models, none of the deleted models
- GTProfile has citation_formats, reference_systems, note_placements, script_variants categories
- All 4 YAML profiles use v2.0.0 enum values throughout
- Profile loading works with no errors or warnings about missing labels
- Validator compatible with v2.0.0 models
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-schema-taxonomy-review-revision/01.1-04-SUMMARY.md`
</output>
