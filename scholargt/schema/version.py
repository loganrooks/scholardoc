"""Schema version constants for ScholarGT.

Every GT file references its schema version for migration and backward compatibility.
Follows semantic versioning: MAJOR.MINOR.PATCH
"""

SCHEMA_VERSION = "2.0.0"
SCHEMA_VERSION_TUPLE = (2, 0, 0)
MIN_SUPPORTED_VERSION = "2.0.0"
